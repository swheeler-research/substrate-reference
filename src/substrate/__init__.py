"""
The Sovereign Substrate: reference implementation.

This package implements the substrate's architectural primitives, the
compile-at-commit pipeline, the runtime, and the federated ledger at the
smallest useful scale.

The architecture is specified in the v1.4 paper. The code here is the
ground truth for what the substrate actually does, where the paper and
the implementation diverge, the paper is wrong (and gets updated to
reflect what implementation learned).
"""

__version__ = "0.1.0"
